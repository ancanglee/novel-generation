[project]
name = "novelgen-gateway-ddb-jobs"
version = "0.1.0"
description = "AgentCore Gateway Lambda target: put/get_checkpoint, advance_scan_cursor"
requires-python = ">=3.12"
dependencies = ["boto3>=1.35"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
