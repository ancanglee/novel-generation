[project]
name = "novelgen-lambda-shared"
version = "0.1.0"
description = "Shared helpers for gateway Lambda handlers (MCP dispatch)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
